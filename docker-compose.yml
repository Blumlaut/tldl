# docker-compose.yml
version: "3.9"
services:
  bot:
    build: .
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      TELEGRAM_TOKEN: ${TELEGRAM_TOKEN}
      DISCORD_MESSAGE_CONTENT_INTENT: ${DISCORD_MESSAGE_CONTENT_INTENT:-false}
      WYOMING_HOST: ${WYOMING_HOST}
      WYOMING_PORT: ${WYOMING_PORT}
      NODE_ENV: production
    restart: unless-stopped

volumes:
  wyoming-cache:
